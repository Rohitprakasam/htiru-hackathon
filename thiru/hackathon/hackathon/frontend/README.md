# CIRIUS Frontend (Vite + React)

This is a scaffolded frontend for the CIRIUS industrial dashboard.

Install

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend at `http://localhost:8000`.
